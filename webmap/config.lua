Config = {
    updateInterval = 5000,  -- How often the server queries information from clients
    displayWeather = false,  -- Enable or disable weather info on the live map (requires weathersync if enabled)
    Allowed = {
        'osp',
        'lspd', 
        'bcso', 
        'safd' 
    }
}